# Ambiente de Execução

* Python 3.11.9

## Instalação

```bash
pip install -r requirements.txt
```

## Execução

Abrir o notebook `.ipynb` e executar as células na ordem em que foram desenvolvidas.

## Dependências

As dependências necessárias para reproduzir a solução estão listadas no arquivo `requirements.txt`.
